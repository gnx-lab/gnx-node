const status=document.querySelector('#status'),detail=document.querySelector('#detail'),$=id=>document.querySelector(id),phases=[...document.querySelectorAll('#steps li')];
async function invoke(method,tailscale_auth_key){try{const r=await fetch('gnx://bridge',{method:'POST',body:JSON.stringify({method,tailscale_auth_key})});const p=await r.json();if(p.error)throw Error(p.error);render(p.progress||p)}catch(e){status.textContent='Could not contact the local service.';detail.textContent=e.message;$('#retry').hidden=false}}
function render(p){status.textContent=p.message||'Node status';detail.textContent=p.error_code?`Code: ${p.error_code}`:'';phases.forEach((el,i)=>el.className=p.percent!=null&&i*20<p.percent?'done':'');$('#restart').hidden=!p.requires_restart;$('#retry').hidden=!p.retryable;if(p.phase==='mesh'){$('#key-label').hidden=false;$('#mesh-key').hidden=false;$('#join').hidden=false}if(p.state==='NODE_READY')$('#deprovision').hidden=false}
$('#provision').addEventListener('click',()=>{$('#provision').hidden=true;$('#cancel').hidden=false;invoke('Provision')});$('#join').addEventListener('click',()=>invoke('JoinMesh',$('#mesh-key').value));$('#retry').addEventListener('click',()=>invoke('Retry'));$('#cancel').addEventListener('click',()=>invoke('Cancel'));$('#deprovision').addEventListener('click',()=>invoke('Deprovision'));window.addEventListener('load',()=>invoke('GetProgress'));
